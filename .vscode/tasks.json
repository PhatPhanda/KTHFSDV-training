{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Git: Fetch every 15 minutes",
            "type": "shell",
            "command": "while true; do git fetch --all --prune; sleep 900; done",
            "isBackground": true,
            "problemMatcher": [],
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}